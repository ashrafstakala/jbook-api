{
  "name": "decoded-cli",
  "version": "1.0.0",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "Ashraf Stakala",
  "license": "ISC",
  "dependencies": {
    "@decoded-cli/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.38",
    "esbuild": "0.8.26",
    "typescript": "^4.7.2",
    "@decoded-cli/local-api": "^1.0.0",
    "commander": "^9.3.0"
  }
}
